kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
    labels:
      workload: database
  - role: worker
    labels:
      workload: backend
  - role: worker
    labels:
      workload: frontend
